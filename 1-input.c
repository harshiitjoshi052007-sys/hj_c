#include<stdio.h>
int main() //input lena hai to scanf use karo
{
    int a;
    scanf("%d", &a); //&-ampercent address of opreator it means that jo value hune di hai vo value a ko supply hogi
    printf("The value of a is %d",a);
    return 0;
}
