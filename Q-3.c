// Write a program to convert Celsius (Centigrade degrees temperature to Fahrenheit.

#include <stdio.h>
int main()
{
    float C, F;
    C = 37.0;
    F = ((9.0 / 5.0) * C) + 32;
    printf("The value in farhenite is %f", F);
    return 0;
}
